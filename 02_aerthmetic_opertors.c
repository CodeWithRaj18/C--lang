#include<stdio.h>
 
int main(){
    int a = 4;
    int b = 3;
    int c = a+b;
    printf("he value of a is %d and the value of b is %d and sum is %d\n ", a, b,c);
    // modulous opertor is used to ge remainder
    printf("the remainder when a is divided by b is : %d\n", a%b);
    // this doesn't work for exponentitation in c 
    return(0);
}