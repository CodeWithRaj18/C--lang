#include<stdio.h> //header files  
int main()
{
     int a;
//  int b = 7.7; // this is not recommended becz 7.7 is not an integrer its is constant for that we have to use float
    scanf("%d", &a);
    printf("the value of a is %d",a);
    
    ;return(0); 
}
